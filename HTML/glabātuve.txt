// REGISTER

if (isset($_POST['register'])) {
    $u = $_POST['username'];
    $p = $_POST['password'];

    if ($user->register($u, $p)) {
        $msg = "Account created! You can now login.";
    } else {
        $msg = "Registration failed.";
    }
}

^^^^^^^^^^^^^^^^^^NO index.php / Pierakstīties.php -----------------------------------------------------


